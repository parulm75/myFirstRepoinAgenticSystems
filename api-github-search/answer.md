What is the role of query parameters in this request?

Query parameter filter out the information the basis of paramter provided

Why do we use response.json() instead of response.text?
because response.json() returns dictionary while response.text returns the object which needs to again converted to dictionary through json.load() function